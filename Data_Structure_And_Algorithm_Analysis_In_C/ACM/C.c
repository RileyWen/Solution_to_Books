#include <stdio.h>
#include <string.h>
int main(){
    char s[1500], q[22];
    int n, lens, lenq, ps = 0, pq = 0;
    scanf("%s%d", s, &n);
    lens = strlen(s);
    while(n--){
        scanf("%s", q);
        lenq = strlen(q);
        while (ps<lenq){

        }
    }
}